'use strict';

// 版本号常量 — 0.2.5 提取为共享配置
// 格式:正式版为主版本号(如 v0.2.6);预发布版追加后缀与构建日期(如 v0.3.0-alpha (Build 202609) 表示 2026 年 9 月构建)
// 主菜单（menuScene 右下角）与 ESC 菜单（escMenuUI 底部）共用，避免两处维护漂移
// 提示:预发布版号（带 -alpha）不会被公告栏识别为"当前版本"，公告列表会回退为按版本升序
//      （详见 docs/公告编写规范.md 第四章排序规则）；发布正式版号后自动恢复"当前版本置顶"。
export const VERSION_TEXT = 'v0.3.0-alpha (Build 202609)';
